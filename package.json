{
  "name": "parking-app",
  "version": "1.0.0",
  "description": "Parking app with combined frontend and backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd parking && npm run build",
    "install-all": "npm install && cd parking && npm install",
    "clean": "rimraf node_modules parking/node_modules package-lock.json parking/package-lock.json"
  },
  "keywords": [
    "parking",
    "react",
    "express",
    "mongodb"
  ],
  "author": "Nithish",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "mongoose": "^8.17.1",
    "axios": "^1.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
